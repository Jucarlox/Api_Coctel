import 'package:flutter/material.dart';

class AlcoholPage extends StatelessWidget {
  const AlcoholPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Bebidas Alcolicas'),
      ),
    );
  }
}

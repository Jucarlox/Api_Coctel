import 'package:flutter/material.dart';

class SinAlcoholPage extends StatelessWidget {
  const SinAlcoholPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Bebidas Sin Alcohol'),
      ),
    );
  }
}
